#!/bin/bash
# use this command to make script executable: chmod +x ./script.txt
trap 'echo "# $BASH_COMMAND"' DEBUG
rootcint -f mainDic.cpp -c  vinogradovpdf.h linkdef.h;
g++ -o main main.cpp vinogradovpdf.cpp mainDic.cpp `root-config --cflags --glibs`;
#./main

#!/bin/bash
# use this command to make script executable: chmod +x ./script.txt
trap 'echo "# $BASH_COMMAND"' DEBUG
rootcint -f mainDic.cpp -c mymainframe.h myworker.h linkdef.h;
g++ -o main mymainframe.cpp myworker.cpp main.cpp mainDic.cpp `root-config --cflags --glibs`;
#./main

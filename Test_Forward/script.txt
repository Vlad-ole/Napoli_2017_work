#!/bin/bash
# use this command to make script executable: chmod +x ./script.txt
trap 'echo "# $BASH_COMMAND"' DEBUG
rootcint -f mainDic.cpp -c a.h b.h linkdef.h;
g++ -o main a.cpp b.cpp main.cpp mainDic.cpp `root-config --cflags --glibs`;
#./main
